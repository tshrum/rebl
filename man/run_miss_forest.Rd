% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_miss_forest.R
\name{run_miss_forest}
\alias{run_miss_forest}
\title{Run MissForest Imputation}
\usage{
run_miss_forest(
  df,
  tuning_grid = tuning_grid,
  verbose = TRUE,
  variablewise = FALSE,
  parallelize = "variables",
  seed = 42,
  ...
)
}
\arguments{
\item{df}{A data frame (cannot be a tibble!) that contains missing data. All
vars must either be factors or numeric. No vars can have a single output.}

\item{tuning_grid}{Data frame with mtry and ntree columns for hyperparameter tuning}

\item{verbose}{Logical, whether to show progress (default: TRUE)}

\item{variablewise}{Logical, whether to compute error estimates per variable (default: FALSE)}

\item{parallelize}{Character, parallelization method (default: 'variables')}

\item{seed}{Integer, random seed for reproducibility (default: 42)}

\item{...}{Additional arguments passed to missForest}
}
\value{
List containing imputed datasets for each parameter combination
}
\description{
Runs missForest algorithm for data imputation using a hyperparameter tuning grid
rather than choosing specific values of mtry and ntree. Default is to run parallel
and set seed as 42 with each iteration.
}
\examples{
\dontrun{
# Example usage
tuning_grid <- expand.grid(mtry = c(2, 4, 6), ntree = c(100, 500))
results <- run_miss_forest(data_with_nas, tuning_grid)
}
}
