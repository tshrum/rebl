% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_rebl_scores.R
\name{get_rebl_scores}
\alias{get_rebl_scores}
\title{Get REBL Scores}
\usage{
get_rebl_scores(model, include_fits = TRUE)
}
\arguments{
\item{model}{A fitted eRm Rasch model object from \code{\link[=get_rasch_model]{get_rasch_model()}}}

\item{include_fits}{Logical indicating whether to include person fit statistics
in the output (default: TRUE)}
}
\value{
A dataframe with participant IDs, REBL scores, and optionally person
fit statistics. When include_fits is TRUE, includes columns for outfit,
infit, and other fit measures from eRm::personfit()
}
\description{
Extract person parameters (REBL scores) from a fitted Rasch model.
Optionally includes person fit statistics to assess how well each person's
response pattern fits the Rasch model.
}
\examples{
\dontrun{
  # Fit model and get scores
  model <- get_rasch_model(df, "id", rebl_items)
  scores <- get_rebl_scores(model, include_fits = TRUE)

  # Get scores without fit statistics
  scores_only <- get_rebl_scores(model, include_fits = FALSE)
}
}
\seealso{
\code{\link[=get_rasch_model]{get_rasch_model()}}, \code{\link[=id_rebl_items]{id_rebl_items()}}, \code{\link[=recode_rebl]{recode_rebl()}},
\code{\link[=reverse_code_rebl_items]{reverse_code_rebl_items()}}
}
